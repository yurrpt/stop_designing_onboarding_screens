import 'package:flutter/material.dart';

class ButtonColors extends Color {
  static const Color kPrimaryColor = Color(0XFF003ba9);
  static const Color whiteColor = Colors.white;

  ButtonColors(super.value);
}

class OnboardTexts {
  static const String data = 'Start using the app';
}
